name: Agent Run — agent-execute

run-name: "#${{ inputs.issue_number }} agent-execute ${{ inputs.issue_title }} (${{ inputs.host }})"

# Phase 2 of the multi-phase agent-run pipeline (glimmung#102 pattern,
# ambience rollout). Runs the Claude agent against the validation env
# env-prep already stood up. validation_url + namespace + image_tag
# arrive as workflow_dispatch inputs threaded through by glimmung's
# runtime substitution path (glimmung#101).
#
# After the agent Job completes this phase rebuilds the validation env
# from the agent branch, runs the Playwright screenshot pass against
# /_styleguide + a few key dev pages, uploads to Azure blob, and fires
# the final completed callback. No outputs declared on the PhaseSpec —
# this is the terminal user phase; glimmung opens the PR.

on:
  workflow_dispatch:
    inputs:
      lease_id:
        description: Glimmung lease ID (ULID)
        required: true
        type: string
      host:
        description: Glimmung slot host (e.g. ambience-slot-1)
        required: true
        type: string
      issue_number:
        description: Issue number to address
        required: true
        type: string
      issue_title:
        description: Issue title (informational)
        required: false
        type: string
      issue_id:
        description: Glimmung Issue id (ULID, optional).
        required: false
        type: string
      gh_event:
        description: Originating GitHub event (informational)
        required: false
        type: string
      gh_action:
        description: Originating GitHub action (informational)
        required: false
        type: string
      run_id:
        description: Glimmung Run ULID (used for branch=glimmung/<run_id>)
        required: true
        type: string
      attempt_index:
        description: 0-based attempt index for this phase.
        required: false
        type: string
      prior_verification_artifact_url:
        description: GHA Actions API URL of the previous attempt's `verification` artifact. Set on retry; the agent prompt is augmented with the prior failure reasons.
        required: false
        type: string
      feedback:
        description: Human feedback text from a reject signal. Set on triage; the agent prompt is augmented with this.
        required: false
        type: string
      pr_number:
        description: Existing PR number to force-push to. Set on triage; no new PR is opened.
        required: false
        type: string
      # ── Inputs threaded from env-prep's outputs (glimmung#101) ──
      validation_url:
        description: Live preview env URL stood up by env-prep.
        required: true
        type: string
      namespace:
        description: K8s namespace env-prep deployed into. Reused for the agent Job + rebuild.
        required: true
        type: string
      image_tag:
        description: Initial image tag env-prep built. Used as the prefix for the rebuild (-r2) tag.
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  issues: write
  pull-requests: write

env:
  GLIMMUNG_BASE: https://glimmung.romaine.life
  GLIMMUNG_PROJECT: ambience
  AGENT_CONTAINER_TAG: latest
  PYTHONUNBUFFERED: "1"
  PYTHONIOENCODING: "utf-8"

jobs:
  route:
    name: Verify glimmung lease
    runs-on: ubuntu-latest
    steps:
      - name: Verify glimmung lease
        env:
          LEASE_ID: ${{ inputs.lease_id }}
          HOST: ${{ inputs.host }}
        run: |
          set -euo pipefail
          resp=$(curl -fsS "${GLIMMUNG_BASE}/v1/lease/${LEASE_ID}?project=${GLIMMUNG_PROJECT}")
          state=$(printf '%s' "$resp" | jq -r .state)
          host=$(printf '%s' "$resp" | jq -r .host)
          if [[ "$state" != "active" ]]; then
            echo "::error::lease ${LEASE_ID} state=${state} (expected active)"
            exit 1
          fi
          if [[ "$host" != "$HOST" ]]; then
            echo "::error::lease ${LEASE_ID} host=${host} (expected ${HOST})"
            exit 1
          fi
          echo "lease verified: ${LEASE_ID} active on ${HOST}"

  agent-execute:
    name: Run agent + screenshots + upload
    needs: route
    runs-on: ubuntu-latest
    env:
      ISSUE_NUMBER: ${{ inputs.issue_number }}
      ISSUE_TITLE: ${{ inputs.issue_title }}
      VALIDATION_URL: ${{ inputs.validation_url }}
      NAMESPACE: ${{ inputs.namespace }}
      IMAGE_TAG: ${{ inputs.image_tag }}
      BRANCH_NAME: glimmung/${{ inputs.run_id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install ambience preview package
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install ./mcp

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.ARM_CLIENT_ID }}
          tenant-id: ${{ vars.ARM_TENANT_ID }}
          subscription-id: ${{ vars.ARM_SUBSCRIPTION_ID }}

      - name: Fetch GitHub App credentials from Key Vault
        env:
          KV_NAME: ${{ vars.KEY_VAULT_NAME }}
        run: |
          set -euo pipefail
          app_id=$(az keyvault secret show --vault-name "$KV_NAME" --name "github-app-id" --query value -o tsv)
          pem=$(az keyvault secret show --vault-name "$KV_NAME" --name "github-app-private-key" --query value -o tsv)
          echo "::add-mask::${app_id}"
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            echo "::add-mask::${line}"
          done <<< "$pem"
          echo "GH_APP_ID=${app_id}" >> "$GITHUB_ENV"
          {
            echo "GH_APP_PRIVATE_KEY<<KEY_EOF"
            echo "$pem"
            echo "KEY_EOF"
          } >> "$GITHUB_ENV"

      - name: Mint GitHub App installation token
        id: app-token
        # 1h TTL — long enough for the agent run + rebuild + evidence
        # capture + screenshot pass.
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ env.GH_APP_ID }}
          private-key: ${{ env.GH_APP_PRIVATE_KEY }}

      - name: Set AKS context
        env:
          AZURE_AKS_RESOURCE_GROUP: ${{ vars.AZURE_AKS_RESOURCE_GROUP }}
          AZURE_AKS_CLUSTER_NAME: ${{ vars.AZURE_AKS_CLUSTER_NAME }}
        run: |
          set -euo pipefail
          az aks get-credentials \
            --resource-group "$AZURE_AKS_RESOURCE_GROUP" \
            --name "$AZURE_AKS_CLUSTER_NAME" \
            --overwrite-existing

      - name: Compute run-scoped variables
        env:
          GLIMMUNG_RUN_ID: ${{ inputs.run_id }}
          ATTEMPT_INDEX: ${{ inputs.attempt_index }}
          PR_NUMBER: ${{ inputs.pr_number }}
        run: |
          set -euo pipefail
          if [ -n "${PR_NUMBER}" ]; then
            mode="recycle"
          else
            mode="initial"
          fi
          echo "branch resolution: mode=${mode} branch=${BRANCH_NAME} attempt=${ATTEMPT_INDEX:-0}"

          {
            echo "JOB_NAME=agent-${IMAGE_TAG}"
            echo "DISPATCH_MODE=${mode}"
            echo "ISSUE_URL=https://github.com/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}"
          } >> "$GITHUB_ENV"

      - name: Notify glimmung — agent-execute started
        # Stamps THIS phase's GHA workflow_run_id on the agent-execute
        # PhaseAttempt. validation_url is already stamped on the Run by
        # env-prep's started callback.
        env:
          GLIMMUNG_RUN_ID: ${{ inputs.run_id }}
        run: |
          set -euo pipefail
          payload=$(jq -nc --argjson rid "${GITHUB_RUN_ID}" '{workflow_run_id: $rid}')
          curl -sS -X POST \
            -H "Content-Type: application/json" \
            -d "${payload}" \
            "${GLIMMUNG_BASE}/v1/runs/${GLIMMUNG_PROJECT}/${GLIMMUNG_RUN_ID}/started" \
            || echo "::warning::failed to post started callback to glimmung; continuing"

      - name: Resolve claude-api-proxy ClusterIP
        run: |
          set -euo pipefail
          ip=$(kubectl -n tank-operator get svc claude-api-proxy -o jsonpath='{.spec.clusterIP}')
          if [ -z "$ip" ]; then
            echo "::error::claude-api-proxy Service not found in tank-operator namespace"
            exit 1
          fi
          echo "PROXY_IP=${ip}" >> "$GITHUB_ENV"

      - name: Build agent prompt with retry/triage context
        env:
          PRIOR_VERIFICATION_ARTIFACT_URL: ${{ inputs.prior_verification_artifact_url }}
          FEEDBACK: ${{ inputs.feedback }}
          PR_NUMBER: ${{ inputs.pr_number }}
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          dest=/tmp/agent-prompt.md
          : > "${dest}"

          if [ -n "${PRIOR_VERIFICATION_ARTIFACT_URL:-}" ]; then
            tmpzip=$(mktemp --suffix=.zip)
            if curl -fsSL -H "Authorization: Bearer ${GH_TOKEN}" \
                -H "Accept: application/vnd.github+json" \
                "${PRIOR_VERIFICATION_ARTIFACT_URL}" -o "${tmpzip}"; then
              tmpdir=$(mktemp -d)
              if unzip -q "${tmpzip}" -d "${tmpdir}" 2>/dev/null \
                  && [ -f "${tmpdir}/verification.json" ]; then
                {
                  echo "## Prior attempt verification — reasons to address"
                  echo ""
                  echo "The previous attempt's verification phase reported these issues."
                  echo "Address each before producing a new commit."
                  echo ""
                  jq -r '.reasons // [] | .[] | "- \(.)"' "${tmpdir}/verification.json" \
                    || echo "- (no reasons enumerated; see prior workflow run for detail)"
                  echo ""
                  if jq -e '.evidence_refs // [] | length > 0' "${tmpdir}/verification.json" >/dev/null; then
                    echo "Prior evidence references:"
                    jq -r '.evidence_refs // [] | .[] | "- \(.)"' "${tmpdir}/verification.json"
                    echo ""
                  fi
                  echo "---"
                  echo ""
                } >> "${dest}"
              else
                echo "::warning::prior verification artifact present but couldn't extract verification.json"
              fi
              rm -rf "${tmpdir}" "${tmpzip}"
            else
              echo "::warning::failed to fetch prior verification artifact ${PRIOR_VERIFICATION_ARTIFACT_URL}"
            fi
          fi

          if [ -n "${FEEDBACK:-}" ]; then
            {
              echo "## Human feedback on the open PR"
              echo ""
              echo "A reviewer rejected the previous attempt with this feedback. The PR"
              echo "branch is already pushed; your job is to address this feedback and"
              echo "force-push back to the same branch."
              if [ -n "${PR_NUMBER:-}" ]; then
                echo " (PR #${PR_NUMBER})"
              fi
              echo ""
              while IFS= read -r line; do
                echo "> ${line}"
              done <<< "${FEEDBACK}"
              echo ""
              echo "---"
              echo ""
            } >> "${dest}"
          fi

          cat .github/agent/prompt.md >> "${dest}"
          echo "agent prompt size: $(wc -l < "${dest}") lines, $(wc -c < "${dest}") bytes"

      - name: Provision agent ConfigMap + GitHub-token Secret
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          kubectl -n "${NAMESPACE}" create configmap agent-config \
            --from-file=prompt.md=/tmp/agent-prompt.md \
            --dry-run=client -o yaml | kubectl apply -f -
          kubectl -n "${NAMESPACE}" create secret generic agent-github-token \
            --from-literal=token="${GITHUB_TOKEN}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply agent Job
        run: |
          set -euo pipefail
          python -m ambience_preview.cli apply-agent-job \
            --namespace "${NAMESPACE}" \
            --job-name "${JOB_NAME}" \
            --issue-number "${ISSUE_NUMBER}" \
            --issue-title "${ISSUE_TITLE}" \
            --issue-url "${ISSUE_URL}" \
            --validation-url "${VALIDATION_URL}" \
            --branch-name "${BRANCH_NAME}" \
            --proxy-ip "${PROXY_IP}" \
            --agent-container-tag "${AGENT_CONTAINER_TAG}"

      - name: Stream + wait for Job
        id: job
        run: |
          set -euo pipefail
          python -m ambience_preview.cli wait-agent-job \
            --namespace "${NAMESPACE}" \
            --job-name "${JOB_NAME}"

      - name: Capture agent event log
        if: always()
        run: |
          set -euo pipefail
          pod=$(kubectl -n "${NAMESPACE}" get pods -l "job-name=${JOB_NAME}" -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || true)
          if [ -z "${pod}" ]; then
            echo "no pod found for job ${JOB_NAME}; skipping event capture"
            : > /tmp/agent-pod.log
            : > /tmp/agent-events.jsonl
            exit 0
          fi
          kubectl -n "${NAMESPACE}" logs "${pod}" > /tmp/agent-pod.log || true
          grep -E '^\{' /tmp/agent-pod.log > /tmp/agent-events.jsonl || true
          echo "captured $(wc -l < /tmp/agent-events.jsonl) event lines from ${pod}"

      - name: Capture agent evidence from pod log
        # Pod streams its evidence tarball to stdout right before exit.
        # Non-fatal: extraction failure leaves the run open without
        # in-pod screenshots — the CI-side Playwright pass below covers
        # the styleguide + a few key dev pages either way.
        id: evidence
        if: success()
        run: |
          set -uo pipefail
          mkdir -p /tmp/evidence
          if grep -q '===EVIDENCE-TAR-START===' /tmp/agent-pod.log; then
            if ! sed -n '/===EVIDENCE-TAR-START===/,/===EVIDENCE-TAR-END===/{//!p;}' \
                /tmp/agent-pod.log \
                | base64 -d 2>/tmp/extract.err \
                | tar -xzf - -C /tmp/evidence 2>>/tmp/extract.err; then
              echo "::warning::evidence tarball extraction failed; continuing without in-pod evidence"
              cat /tmp/extract.err >&2 || true
              rm -rf /tmp/evidence
              mkdir -p /tmp/evidence
            fi
          else
            echo "::warning::no evidence tar markers found in agent pod log"
          fi
          echo "evidence files extracted:"
          find /tmp/evidence -type f 2>/dev/null | sort || true
          have_notes=false
          if [ -s /tmp/evidence/notes.md ]; then
            have_notes=true
          fi
          deep_validation_url=""
          if [ -s /tmp/evidence/validation-path.txt ]; then
            path=$(head -1 /tmp/evidence/validation-path.txt | tr -d '[:space:]')
            case "${path}" in
              /*) deep_validation_url="${VALIDATION_URL}${path}" ;;
              "") ;;
              *) echo "::warning::validation-path.txt content '${path}' missing leading slash; falling back to bare host" ;;
            esac
          fi
          echo "have_notes=${have_notes}" >> "$GITHUB_OUTPUT"
          echo "deep_validation_url=${deep_validation_url}" >> "$GITHUB_OUTPUT"

      - name: Fetch agent branch onto runner
        if: success()
        run: |
          set -euo pipefail
          git fetch origin "${BRANCH_NAME}"
          git checkout -B "${BRANCH_NAME}" "origin/${BRANCH_NAME}"

      - name: Rebuild validation env from agent branch
        # The validation-env URL surfaced in the PR body must reflect the
        # diff, not the pre-agent build. -r2 suffix keeps the rebuild
        # auditable + tear-down can reclaim both.
        if: success()
        run: |
          set -euo pipefail
          rebuild_tag="${IMAGE_TAG}-r2"
          echo "REBUILT_IMAGE_TAG=${rebuild_tag}" >> "$GITHUB_ENV"
          python -m ambience_preview.cli rebuild-validation-image \
            --namespace "${NAMESPACE}" \
            --branch "${BRANCH_NAME}" \
            --image-tag "${rebuild_tag}"

      - name: Wait for rebuilt validation env
        if: success()
        run: |
          set -euo pipefail
          python -m ambience_preview.cli wait-public-preview --url "${VALIDATION_URL}"

      - name: Capture screenshot pass
        # Playwright runs against the rebuilt env. Page list lives in
        # screenshot-pages.json so an agent adding a route just appends
        # without touching CI. Failure here is a typed abort — empty or
        # broken pages should fail loud, not silently produce empty PRs.
        id: screenshots
        if: success()
        env:
          BASE_URL: ${{ env.VALIDATION_URL }}
          PAGES_JSON: screenshot-pages.json
          OUT_DIR: /tmp/evidence/screenshots
          GLIMMUNG_RUN_ID: ${{ inputs.run_id }}
        run: |
          set -euo pipefail
          if [ ! -f "${PAGES_JSON}" ]; then
            echo "::warning::no ${PAGES_JSON}; skipping screenshot pass"
            echo "have_screenshots=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          mkdir -p "${OUT_DIR}"

          npm init -y >/dev/null
          npm install --no-save --silent playwright@1
          npx --yes playwright install chromium

          set +e
          node scripts/screenshot-pages.mjs > /tmp/screenshot-stdout.log 2> /tmp/screenshot-stderr.log
          rc=$?
          set -e
          cat /tmp/screenshot-stdout.log || true
          cat /tmp/screenshot-stderr.log >&2 || true

          if [ "${rc}" -ne 0 ]; then
            reason=$(head -1 /tmp/screenshot-stderr.log | sed 's/;.*$//') || true
            if [ -z "${reason}" ]; then
              reason="screenshot_capture_failed: <unknown> (script exit ${rc})"
            fi
            echo "::error::${reason}"
            if [ -n "${GLIMMUNG_RUN_ID}" ]; then
              payload=$(jq -nc --arg r "${reason}" '{reason: $r}')
              curl -sS -X POST \
                -H "Content-Type: application/json" \
                -d "${payload}" \
                "${GLIMMUNG_BASE}/v1/runs/${GLIMMUNG_PROJECT}/${GLIMMUNG_RUN_ID}/aborted" \
                || echo "::warning::failed to post typed abort to glimmung; lease-release will still fire"
            fi
            exit 1
          fi

          have_screenshots=false
          if compgen -G "${OUT_DIR}/*.png" >/dev/null; then
            have_screenshots=true
          fi
          echo "have_screenshots=${have_screenshots}" >> "$GITHUB_OUTPUT"

      - name: Refresh Azure OIDC login
        # azure/login@v2's OIDC JWT is valid for 5 minutes only. Re-mint
        # before the upload step so `az storage` doesn't AADSTS700024.
        if: success() && steps.screenshots.outputs.have_screenshots == 'true'
        uses: azure/login@v2
        with:
          client-id: ${{ vars.ARM_CLIENT_ID }}
          tenant-id: ${{ vars.ARM_TENANT_ID }}
          subscription-id: ${{ vars.ARM_SUBSCRIPTION_ID }}

      - name: Upload screenshots to Azure blob
        id: upload
        if: success() && steps.screenshots.outputs.have_screenshots == 'true'
        continue-on-error: true
        env:
          STORAGE_ACCOUNT: ${{ vars.AGENT_SCREENSHOT_STORAGE_ACCOUNT }}
          CONTAINER: ${{ vars.AGENT_SCREENSHOT_CONTAINER }}
          CONTAINER_URL: ${{ vars.AGENT_SCREENSHOT_CONTAINER_URL }}
          MAX_SCREENSHOTS: "20"
        run: |
          set -euo pipefail
          prefix="issue-${ISSUE_NUMBER}/${GITHUB_RUN_ID}"

          staging=$(mktemp -d)
          mapfile -t pngs < <(find /tmp/evidence/screenshots -maxdepth 1 -type f -name '*.png' | sort)
          total=${#pngs[@]}
          taken=0
          for f in "${pngs[@]}"; do
            [ "${taken}" -lt "${MAX_SCREENSHOTS}" ] || break
            cp "${f}" "${staging}/"
            taken=$((taken + 1))
          done
          echo "uploading ${taken}/${total} screenshots (cap ${MAX_SCREENSHOTS})"

          upload_ok=true
          if ! az storage blob upload-batch \
              --account-name "${STORAGE_ACCOUNT}" \
              --destination "${CONTAINER}" \
              --destination-path "${prefix}" \
              --source "${staging}" \
              --auth-mode login \
              --overwrite true; then
            upload_ok=false
            echo "::warning::screenshot upload failed; PR body will note this"
          fi

          {
            echo "markdown<<EOF_MD"
            echo "## Screenshots"
            echo ""
            if [ "${upload_ok}" = "false" ]; then
              echo "_Screenshot upload failed — see [run logs](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID})._"
              echo ""
            else
              if [ "${total}" -gt "${taken}" ]; then
                echo "_Showing first ${taken} of ${total} screenshots._"
                echo ""
              fi
              for f in "${staging}"/*.png; do
                [ -e "${f}" ] || continue
                base=$(basename "${f}")
                echo "### ${base%.png}"
                echo ""
                echo "![${base%.png}](${CONTAINER_URL}/${prefix}/${base})"
                echo ""
              done
            fi
            echo "EOF_MD"
          } >> "$GITHUB_OUTPUT"

      - name: Label validation env namespace by branch
        # agent-pr-cleanup.yml looks up namespaces by `ambience.io/branch=<slug>`
        # to reap them on PR close. Slug = BRANCH_NAME with `/` → `-`.
        if: success() && inputs.pr_number == ''
        run: |
          set -euo pipefail
          slug="${BRANCH_NAME//\//-}"
          if ! kubectl label namespace "${NAMESPACE}" "ambience.io/branch=${slug}" --overwrite; then
            echo "::warning::failed to label namespace ${NAMESPACE} with ambience.io/branch=${slug}; agent-pr-cleanup may not find it on PR close"
          fi

      - name: Surface triage outcome on existing PR
        if: success() && inputs.pr_number != ''
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_NUMBER: ${{ inputs.pr_number }}
          HAVE_NOTES: ${{ steps.evidence.outputs.have_notes }}
          SCREENSHOTS_MD: ${{ steps.upload.outputs.markdown }}
        run: |
          set -euo pipefail
          body_file=$(mktemp)
          {
            echo "Triage attempt force-pushed to \`${BRANCH_NAME}\`. New evidence below."
            echo ""
            if [ "${HAVE_NOTES}" = "true" ]; then
              echo "### Agent notes"
              echo ""
              cat /tmp/evidence/notes.md
              echo ""
            fi
            if [ -n "${SCREENSHOTS_MD:-}" ]; then
              printf '%s\n' "${SCREENSHOTS_MD}"
              echo ""
            fi
            echo "_Triage run on host \`${{ inputs.host }}\` (lease \`${{ inputs.lease_id }}\`)._"
          } > "${body_file}"
          gh pr comment "${PR_NUMBER}" --body-file "${body_file}" \
            || echo "::warning::failed to post triage outcome comment on PR #${PR_NUMBER}"
          echo "PR_NUMBER=${PR_NUMBER}" >> "$GITHUB_ENV"

      - name: Summarize agent run
        if: always()
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          JOB_STATUS: ${{ job.status }}
        run: |
          set -euo pipefail
          events=/tmp/agent-events.jsonl
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          body_file=$(mktemp)

          if [ -s "${events}" ]; then
            cost=$(jq -r 'select(.type=="result") | .total_cost_usd // 0' "${events}" \
              | awk '{s+=$1} END {printf "%.4f", s}')
            turns=$(jq -r 'select(.type=="result") | .num_turns // 0' "${events}" \
              | awk '{s+=$1} END {printf "%d", s}')
            input=$(jq -r 'select(.type=="result") | .usage.input_tokens // 0' "${events}" \
              | awk '{s+=$1} END {printf "%d", s}')
            output=$(jq -r 'select(.type=="result") | .usage.output_tokens // 0' "${events}" \
              | awk '{s+=$1} END {printf "%d", s}')
            cache_create=$(jq -r 'select(.type=="result") | .usage.cache_creation_input_tokens // 0' "${events}" \
              | awk '{s+=$1} END {printf "%d", s}')
            cache_read=$(jq -r 'select(.type=="result") | .usage.cache_read_input_tokens // 0' "${events}" \
              | awk '{s+=$1} END {printf "%d", s}')
            tools=$(jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="tool_use") | .name' "${events}" \
              | wc -l | tr -d ' ')
            tool_breakdown=$(jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="tool_use") | .name' "${events}" \
              | sort | uniq -c | sort -rn | awk '{printf "  - %s: %d\n", $2, $1}')
            {
              echo "## Agent run summary"
              echo ""
              echo "| Metric | Value |"
              echo "| --- | ---: |"
              echo "| Status | ${JOB_STATUS} |"
              echo "| Run | [${GITHUB_RUN_ID}](${run_url}) |"
              echo "| Issue | #${ISSUE_NUMBER} |"
              echo "| Cost | \$${cost} |"
              echo "| Turns | ${turns} |"
              echo "| Tool calls | ${tools} |"
              echo "| Input tokens | ${input} |"
              echo "| Output tokens | ${output} |"
              echo "| Cache creation tokens | ${cache_create} |"
              echo "| Cache read tokens | ${cache_read} |"
              echo ""
              if [ -n "${tool_breakdown}" ]; then
                echo "### Tool calls"
                echo ""
                printf '%s\n' "${tool_breakdown}"
              fi
            } > "${body_file}"
          else
            {
              echo "## Agent run summary"
              echo ""
              echo "| Metric | Value |"
              echo "| --- | ---: |"
              echo "| Status | ${JOB_STATUS} |"
              echo "| Run | [${GITHUB_RUN_ID}](${run_url}) |"
              echo "| Issue | #${ISSUE_NUMBER} |"
              echo ""
              echo "_No agent events captured — see run logs for failure detail._"
            } > "${body_file}"
          fi

          cat "${body_file}" >> "${GITHUB_STEP_SUMMARY}"
          if [ -n "${ISSUE_NUMBER:-}" ]; then
            gh issue comment "${ISSUE_NUMBER}" --body-file "${body_file}" \
              || echo "::warning::failed to post summary on issue #${ISSUE_NUMBER}; continuing"
          fi
          if [ -n "${PR_NUMBER:-}" ]; then
            gh pr comment "${PR_NUMBER}" --body-file "${body_file}" \
              || echo "::warning::failed to post summary comment on PR #${PR_NUMBER}; continuing"
          fi

      - name: Notify glimmung — run completed
        # Terminal phase: no `outputs` declared on the PhaseSpec, so the
        # payload omits `outputs`. screenshots_markdown lands on the Run
        # for the PR-body composer.
        if: success()
        env:
          GLIMMUNG_RUN_ID: ${{ inputs.run_id }}
          SCREENSHOTS_MD: ${{ steps.upload.outputs.markdown }}
        run: |
          set -euo pipefail
          payload=$(jq -nc \
            --argjson rid "${GITHUB_RUN_ID}" \
            --arg conclusion "success" \
            --arg shots "${SCREENSHOTS_MD}" \
            '{workflow_run_id: $rid, conclusion: $conclusion, screenshots_markdown: ($shots | select(length > 0))}')
          curl -sS -X POST \
            -H "Content-Type: application/json" \
            -d "${payload}" \
            "${GLIMMUNG_BASE}/v1/runs/${GLIMMUNG_PROJECT}/${GLIMMUNG_RUN_ID}/completed" \
            || echo "::warning::failed to post completed callback to glimmung; continuing"

      - name: Tear down validation env on failure
        # Successful runs leave the env alive — agent-pr-cleanup.yml
        # reaps it when the PR closes (matched by ambience.io/branch=<slug>
        # set above). This step only runs on failure / cancellation.
        if: failure() || cancelled()
        run: |
          set -euo pipefail
          python -m ambience_preview.cli destroy-validation-preview \
            --namespace "${NAMESPACE}" \
            --release ambience-agent || true
          kubectl delete namespace "${NAMESPACE}" --ignore-not-found=true --wait=false || true
          if [ -n "${IMAGE_TAG:-}" ]; then
            az acr repository delete --name romainecr --image "ambience:${IMAGE_TAG}" --yes || true
          fi
          if [ -n "${REBUILT_IMAGE_TAG:-}" ]; then
            az acr repository delete --name romainecr --image "ambience:${REBUILT_IMAGE_TAG}" --yes || true
          fi

      - name: Emit verification.json (#18 contract)
        # Always emit a verification artifact so glimmung's verify-loop
        # has a typed verdict to decide on. Schema mirrors what
        # agent-run.yml emitted before the split.
        if: always()
        env:
          JOB_STATUS: ${{ job.status }}
          ATTEMPT_INDEX: ${{ inputs.attempt_index }}
          DISPATCH_MODE: ${{ env.DISPATCH_MODE }}
        run: |
          set +e
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

          status="error"
          reasons='[]'
          if [ "${JOB_STATUS}" = "success" ]; then
            status="pass"
          elif [ "${JOB_STATUS}" = "failure" ]; then
            status="fail"
            reasons_arr=()
            if [ -s /tmp/agent-pod.log ]; then
              while IFS= read -r line; do
                [ -z "${line}" ] && continue
                trimmed="${line:0:200}"
                reasons_arr+=("${trimmed}")
                if [ ${#reasons_arr[@]} -ge 5 ]; then break; fi
              done < <(grep -E "::error::|Job failed|FATAL|panic:" /tmp/agent-pod.log 2>/dev/null | head -5)
            fi
            if [ ${#reasons_arr[@]} -eq 0 ]; then
              reasons_arr+=("agent step exited with non-success status (see ${run_url})")
            fi
            reasons=$(printf '%s\n' "${reasons_arr[@]}" | jq -Rs 'split("\n")[:-1]')
          elif [ "${JOB_STATUS}" = "cancelled" ]; then
            status="error"
            reasons='["agent run was cancelled"]'
          fi

          cost_usd=0
          if [ -s /tmp/agent-events.jsonl ]; then
            cost_usd=$(jq -r 'select(.type=="result") | .total_cost_usd // 0' /tmp/agent-events.jsonl \
              | awk '{s+=$1} END {if (NR>0) printf "%.4f", s; else printf "0"}')
          fi

          evidence_refs='[]'
          if [ -d /tmp/evidence/screenshots ]; then
            evidence_refs=$(find /tmp/evidence/screenshots -maxdepth 1 -type f -name '*.png' \
              -printf 'screenshots/%f\n' 2>/dev/null \
              | jq -Rs 'split("\n")[:-1]')
          fi

          metadata=$(jq -n \
            --arg run_url "${run_url}" \
            --arg dispatch_mode "${DISPATCH_MODE:-initial}" \
            --arg attempt_index "${ATTEMPT_INDEX:-0}" \
            '{
              run_url: $run_url,
              dispatch_mode: $dispatch_mode,
              attempt_index: ($attempt_index | tonumber? // 0)
            }')

          jq -n \
            --arg status "${status}" \
            --argjson reasons "${reasons}" \
            --argjson evidence_refs "${evidence_refs}" \
            --argjson cost_usd "${cost_usd:-0}" \
            --argjson metadata "${metadata}" \
            '{
              schema_version: 1,
              status: $status,
              reasons: $reasons,
              evidence_refs: $evidence_refs,
              cost_usd: $cost_usd,
              prompt_version: "ambience-v1",
              metadata: $metadata
            }' > /tmp/verification.json

      - name: Upload verification artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verification
          path: /tmp/verification.json
          retention-days: 30
          if-no-files-found: error

  release-lease:
    name: Release glimmung lease
    needs: [route, agent-execute]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Release lease
        run: |
          set -euo pipefail
          curl -fsS -X POST "${GLIMMUNG_BASE}/v1/lease/${{ inputs.lease_id }}/release?project=${GLIMMUNG_PROJECT}" >/dev/null
