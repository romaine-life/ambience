apiVersion: v1
kind: Service
metadata:
  name: ambience-authority
  namespace: ambience
spec:
  clusterIP: None
  selector:
    app: ambience
    component: authority
  ports:
    - name: http
      port: 80
      targetPort: http
